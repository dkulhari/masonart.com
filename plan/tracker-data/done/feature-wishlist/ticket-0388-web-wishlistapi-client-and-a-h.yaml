ticket_number: 388
title: Web - wishlistApi client and a hydration-safe useWishlist store
description: |-
  ## Task

  Client half of the wishlist: an API client and a store the components can toggle against optimistically.

  **`wishlistApi`** in `packages/web/app/lib/api.ts`, alongside `cartApi` (:413) and `notificationPreferencesApi` (:1904). Same shape as its neighbours.

  **`packages/web/app/stores/wishlist.ts`** — Zustand, mirroring `stores/cart.ts`:
  - `useWishlistIds()` — the id set, for `ProductCard` to ask "am I saved?" without a request per card.
  - `useWishlistCount()` — for the header badge.
  - `useWishlistHydration()` — same shape as `useCartHydration` (cart.ts:458).
  - `toggleWishlist(productId)` — optimistic: flip local state, fire the request, roll back on failure.

  ## Context

  Feature: wishlist (mesonart parity, PLAN-OF-ACTION feature 1)
  Blocked by the API ticket.

  **Why hydration-safe**: #498 was a cart badge SSR mismatch caused by rendering a count the server could not know. `Header.tsx` already guards it with `const displayCartCount = isHydrated ? cartItemCount : 0`. The wishlist badge does the same.

  **Why an id set rather than per-card requests**: the grid renders up to 24 cards; each asking the server whether it is saved is 24 requests. Fetch once, hold the set.

  **Difference from the cart store**: cart persists to localStorage via the `persist` middleware because a guest cart is real. The wishlist is server-owned and auth-gated — do NOT persist it, or a signed-out user sees the previous user's hearts.
feature: wishlist
type: task
priority: high
status: done
assignee: unassigned
reporter: dhruv
created: '2026-08-04T07:09:43.897Z'
last_updated: '2026-08-04T07:18:12.312Z'
time_spent_minutes: 0
labels:
  - frontend
  - state
acceptance_criteria: []
comments:
  - author: system
    timestamp: '2026-08-04T07:15:55.013Z'
    text: Status changed from 'todo' to 'in-progress'
  - author: system
    timestamp: '2026-08-04T07:18:12.312Z'
    text: Status changed from 'in-progress' to 'done'
work_sessions: []
related_tickets: []
blocked_by: []
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: 'Write failing store tests: optimistic toggle, rollback on failure, hydration gate, no localStorage persistence'
    file: packages/web/tests/stores/wishlist.test.ts
    command: cd packages/web && bunx vitest run tests/stores/wishlist.test.ts
    expected_result: FAIL
    status: pending
  - id: 2
    action: verify-fail
    description: Confirm the suite fails
    command: cd packages/web && bunx vitest run tests/stores/wishlist.test.ts
    expected_result: FAIL - store module does not exist
    status: pending
  - id: 3
    action: implement
    description: Add wishlistApi to lib/api.ts and create the store
    file: packages/web/app/stores/wishlist.ts
    status: pending
  - id: 4
    action: verify-pass
    description: Confirm store tests pass and the web suite is unchanged
    command: cd packages/web && bunx vitest run tests/stores/ tests/lib/api.test.ts
    expected_result: PASS
    status: pending
  - id: 5
    action: commit
    description: Commit the client and store
    command: 'git add packages/web/app/lib/api.ts packages/web/app/stores/wishlist.ts packages/web/tests/stores/wishlist.test.ts && git commit -m "feat(web): wishlist API client and hydration-safe store"'
    status: pending
current_step: 1
started: '2026-08-04T07:15:55.013Z'
completed: '2026-08-04T07:18:12.312Z'
