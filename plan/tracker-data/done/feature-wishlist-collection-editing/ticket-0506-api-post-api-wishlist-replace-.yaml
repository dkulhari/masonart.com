ticket_number: 506
title: API - POST /api/wishlist/replace, the explicit set change
description: |-
  ## Task

  `POST /api/wishlist/replace` takes `{ productIds }` and writes it to `users.wishlist_product_ids` wholesale — a different set, in a given order.

  ## Why this cannot just be PUT

  `PUT /api/wishlist` **refuses any change to the set**. Its permutation guard exists so a stale tab cannot silently drop an item saved on another device (#500), and loading a collection into the wishlist is exactly a set change.

  Loosening the guard to accommodate this would reopen the hole it closed. Two operations, two endpoints: reordering stays absolutely guarded, replacement is opt-in and named for what it does.

  Validate every id is a real product before writing, the way `PUT /:id/products` does — a wishlist full of ids matching nothing renders as an empty page with a non-zero badge.

  ## Context

  Feature: wishlist-collection-editing, Phase 1.

  `packages/api/src/routes/wishlist.ts`. Register BEFORE `/:productId` — both are POST, Hono matches in registration order, and `replace` is not a uuid, so the param route would answer 400. `/merge` carries the same warning for the same reason; read it first.
feature: wishlist-collection-editing
type: task
priority: high
status: done
assignee: unassigned
reporter: dhruv
created: '2026-08-05T20:51:19.065Z'
last_updated: '2026-08-05T20:54:28.786Z'
time_spent_minutes: 0
labels:
  - backend
  - api
acceptance_criteria: []
comments:
  - author: system
    timestamp: '2026-08-05T20:52:21.087Z'
    text: Status changed from 'todo' to 'in-progress'
  - author: system
    timestamp: '2026-08-05T20:54:12.132Z'
    text: Status changed from 'in-progress' to 'done'
  - author: mcp-client
    timestamp: '2026-08-05T20:54:28.786Z'
    text: |-
      ✅ **Completed**

      **Commit**: `3a624412` — feat(api): POST /api/wishlist/replace

      **Files Changed**:
      - `packages/api/src/routes/wishlist.ts` — the handler, registered above `/:productId`
      - `packages/api/tests/routes/wishlist-replace.test.ts` — 10 tests

      **The point of a second endpoint, verified rather than argued**

      `PUT /api/wishlist` refuses any change to the set, and that refusal is load-bearing — it is what stops a stale tab dropping an item saved on another device (#500). Loading a collection into the wishlist is exactly a set change, so it needs its own door.

      Live proof the split holds, same payload both ways:

      ```
      PUT   different set -> 409
      POST  /replace      -> 200
      before: Emerald Dreams | Urban Soul | Paper Layers | Synthetic Nature
      after : Blush Hour | Emerald Dreams | Urban Soul
      unknown id -> 400 (names 1)
      duplicates -> 400
      ```

      Worth stating plainly because the obvious "simplification" here is to loosen the `PUT` guard and delete this endpoint. That would reopen the data-loss hole. The doc comment says so at the handler.

      **Two behaviours that differ from `PUT` on purpose**
      - **An empty list is legitimate** — it clears the wishlist. On `PUT`, an empty list against a non-empty wishlist is a stale write and gets 409.
      - **Ids are checked against the catalogue**, not just against what is already saved. `PUT` only permutes what is there, so its members are known-good by construction; `replace` accepts arbitrary ids and a wishlist full of ones matching nothing renders as an empty page with a non-zero badge.

      **Verification**
      - Command: `cd packages/api && npx vitest run tests/routes/wishlist.test.ts tests/routes/wishlist-reorder.test.ts tests/routes/wishlist-replace.test.ts`
      - Result: PASS — 52 tests, 0 failing
      - Build: `npx tsc --noEmit` — clean

      The routing test (`is not shadowed by POST /:productId`) is there because the failure mode is a 400 that reads as bad input rather than as a registration-order mistake — the third time that trap has come up in this codebase, after `/merge` and `/facets`.
work_sessions: []
related_tickets: []
blocked_by: []
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: Write failing tests
    file: packages/api/tests/routes/wishlist-replace.test.ts
    command: cd packages/api && npx vitest run tests/routes/wishlist-replace.test.ts
    expected_result: FAIL
    status: pending
    code: 'Cases: 401 unauthenticated; writes a completely different set in the given order; an empty list clears the wishlist (this is a legitimate replace, unlike on PUT); ids that are not real products are rejected 400 with nothing written; duplicates rejected; the 500-item cap applies; the route is NOT shadowed by POST /:productId.'
  - id: 2
    action: verify-fail
    description: Confirm failure
    command: cd packages/api && npx vitest run tests/routes/wishlist-replace.test.ts
    expected_result: FAIL
    status: pending
  - id: 3
    action: implement
    description: Add the handler above /:productId
    file: packages/api/src/routes/wishlist.ts
    status: pending
    code: Comment WHY this exists separately from PUT, or the next reader will merge them.
  - id: 4
    action: verify-pass
    description: Confirm all three wishlist suites pass
    command: cd packages/api && npx vitest run tests/routes/wishlist.test.ts tests/routes/wishlist-reorder.test.ts tests/routes/wishlist-replace.test.ts
    expected_result: PASS
    status: pending
  - id: 5
    action: commit
    description: Commit
    command: 'git add packages/api/src/routes/wishlist.ts packages/api/tests/routes/wishlist-replace.test.ts && git commit -m "feat(api): POST /api/wishlist/replace (#TICKET)"'
    status: pending
current_step: 1
started: '2026-08-05T20:52:21.087Z'
verification:
  tests_passed: true
  test_command: cd packages/api && npx vitest run tests/routes/wishlist.test.ts tests/routes/wishlist-reorder.test.ts tests/routes/wishlist-replace.test.ts
  test_output: '52 tests passing across the three wishlist suites, 10 of them new. Live check confirmed the split: the same different-set payload returns 409 from PUT and 200 from POST /replace, with unknown ids 400 (naming them) and duplicates 400.'
  build_passed: true
  build_command: cd packages/api && npx tsc --noEmit -p tsconfig.json
  verified_at: '2026-08-05T20:54:07.244Z'
completed: '2026-08-05T20:54:12.132Z'
