ticket_number: 728
title: Dispatch Library - buyLabelForOrder in one transaction, readiness checked inside it
description: |-
  ## Task

  `packages/api/src/lib/shipment-dispatch.ts`, exporting `buyLabelForOrder(orderId)`.

  ## The whole point is the transaction

  One transaction, `FOR UPDATE` on the order's production job rows, and `isOrderReadyToLabel(orderId, tx)` evaluated **inside** it. Checking readiness outside the lock is the concurrency bug this ticket exists to prevent: two admins pressing Ship at once both read "ready" and both buy a label, and the second one costs money and confuses a courier.

  `lib/shipment-*` is already admitted in `LABEL_READINESS_CONSUMERS` (`production-readiness.ts:858`) with exactly this reason recorded, so the seam scan will accept it. Do not widen the allow-list.

  ## Shape

  1. Open the transaction, `FOR UPDATE` the job rows.
  2. `isOrderReadyToLabel(orderId, tx)` — refuse with the blockers if not.
  3. Refuse if a live label already exists. The partial unique index from #703 enforces at most one live labelled shipment per order; do not rely on it alone for the message.
  4. Buy through the phase 6 client: create order, assign AWB, fetch label.
  5. Store the PDF via `lib/storage.ts` `uploadFile` at `fulfilment/labels/<token>.pdf`, set `label_object_token`.
  6. Audit it.

  ## Audit

  `shipment.created` and `shipment.tracking_updated` are already declared. Buying a label needs its own action — **declare and emit it in this same ticket.** #671 fails the build on any declared action nothing emits, so a declaration landing a ticket ahead of its emitter breaks the build for everyone.

  ## The network call is the hard part

  A courier API call inside a database transaction holds locks across the network. Decide deliberately and write the reasoning into the file: either keep the transaction open (simple, but a slow Shiprocket holds row locks) or split it (lock, mark intent, commit, call, reconcile). The second is more correct and more work. Whichever you choose, the failure mode you must not ship is a bought label with no row recording it.

  ## Done when

  A concurrency test proves two simultaneous calls produce exactly one label. Removing `FOR UPDATE` must make it fail — verify that by actually removing it, the way `order-shipments-live-label.test.ts` proves its index.
feature: order-dispatch-tracking
type: task
priority: critical
status: todo
assignee: unassigned
reporter: dhruv
created: '2026-08-31T13:49:58.656Z'
last_updated: '2026-08-31T14:07:54.136Z'
time_spent_minutes: 0
labels:
  - shiprocket
  - api
  - pass-2
  - phase-7
acceptance_criteria: []
comments:
  - author: mcp-client
    timestamp: '2026-08-31T14:07:54.136Z'
    text: |-
      📌 **Decision: split the transaction. Do not hold locks across the courier call.**

      Dhruv chose the second of the two options the ticket left open (2026-08-31).

      So the shape is **lock → mark intent → commit → call → reconcile**, not one long transaction wrapped around the Shiprocket request.

      **Why it is the more correct one.** A courier API call inside an open transaction holds `FOR UPDATE` locks on the order's job rows for as long as Shiprocket takes to answer. That is an external service with no latency guarantee: a slow response blocks every other reader of those rows, and a hung one holds them until the statement timeout. On a pool this app shares with a dev box running many sessions, that is how one dispatch takes the database down.

      **What must still hold.** The concurrency property does not weaken: two admins pressing Ship at once must still produce exactly one label. That now comes from the *intent row* committed in step 2, not from a lock held through step 4 — a second caller finds the intent already recorded and refuses, instead of waiting on a lock.

      **The failure mode this must not ship**, unchanged from the ticket: a bought label with no row recording it. Splitting the transaction makes this *more* likely, not less, because the commit and the purchase are now separate events. So the intent row has to be written **before** the call and reconciled **after** it, and a crash between the two must leave a row that a later reconcile can recognise as "we may have bought this — go and check" rather than as either done or never-started.

      That reconcile path is part of this ticket, not a follow-up. An intent row with no reconciliation is worse than the long transaction it replaced.

      **Test accordingly.** The concurrency test still has to prove one label from two simultaneous callers, and it must fail when the intent row is removed — proved by actually removing it, the way `order-shipments-live-label.test.ts` proves its index rather than asserting the index is merely defined.
work_sessions: []
related_tickets: []
blocked_by:
  - ticket: 725
    feature: order-dispatch-tracking
    description: Shiprocket Client - Serviceability and choosing a courier
  - ticket: 726
    feature: order-dispatch-tracking
    description: Shiprocket Client - Create the order and assign an AWB
  - ticket: 727
    feature: order-dispatch-tracking
    description: Shiprocket Client - Fetch the label PDF and schedule a pickup
blocks: []
attachments: []
