ticket_number: 323
title: Backend API - requireContentManager middleware and isContentManager helper
description: |-
  ## Task
  Add `requireContentManager` middleware (allows content-manager, admin, super-admin) and `isContentManager` helper to the auth middleware module.

  ## Context
  Part of feature: content-manager-role
  Phase: Backend API

  Follow the exact existing pattern: `requireAdmin` at `packages/api/src/middleware/auth.ts:262` is `requireRole(["admin", "super-admin"])`. Unit tests live in `packages/api/tests/middleware/auth.test.ts` — follow the existing `requireAdmin`/`requireRole` describe blocks (they use `createMockUser` fixture + Hono test app).

  Do NOT touch `requireTrade`, `isAdmin`, or `canAccess` — content-manager gets no trade access and is not an admin (per design decision 4).
feature: content-manager-role
type: task
priority: medium
status: done
assignee: unassigned
reporter: cli
created: "2026-07-23T11:49:08.940Z"
last_updated: "2026-07-23T12:02:44.122Z"
time_spent_minutes: 0
labels:
  - backend
  - api
acceptance_criteria: []
comments:
  - author: system
    timestamp: "2026-07-23T12:01:39.507Z"
    text: Status changed from 'todo' to 'in-progress'
  - author: system
    timestamp: "2026-07-23T12:02:44.123Z"
    text: Status changed from 'in-progress' to 'done'
  - author: mcp-client
    timestamp: "2026-07-23T12:02:45.404Z"
    text: "✅ Implemented in commit 02358ffa. TDD: 13 new tests red (requireContentManager/isContentManager not exported) → green after adding both to middleware/auth.ts following the requireAdmin/isAdmin patterns. Full middleware suite: 131/131 passing."
work_sessions: []
related_tickets: []
blocked_by:
  - ticket: 322
    feature: content-manager-role
    description: Types & Schema - Add content-manager to role enums and migration
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: "Write failing unit tests: requireContentManager allows content-manager/admin/super-admin, rejects customer and trade with 403; isContentManager helper truth table. Follow the existing requireAdmin describe block pattern (createMockUser + test Hono app)."
    file: packages/api/tests/middleware/auth.test.ts
    command: cd packages/api && bunx vitest run tests/middleware/auth.test.ts
    expected_result: FAIL
    status: pending
    code: |-
      describe('requireContentManager', () => {
        it.each(['content-manager', 'admin', 'super-admin'] as const)(
          'allows %s role', async (role) => {
            // mirror requireAdmin test: app with requireContentManager, mock user with { role }
            // expect 200
          });
        it.each(['customer', 'trade'] as const)(
          'rejects %s role with 403', async (role) => {
            // expect 403 FORBIDDEN
          });
      });

      describe('isContentManager', () => {
        it('returns true for content-manager, admin, super-admin', () => {
          expect(isContentManager(createMockUser({ role: 'content-manager' }))).toBe(true);
          expect(isContentManager(createMockUser({ role: 'admin' }))).toBe(true);
          expect(isContentManager(createMockUser({ role: 'super-admin' }))).toBe(true);
        });
        it('returns false for customer, trade, null', () => {
          expect(isContentManager(createMockUser({ role: 'customer' }))).toBe(false);
          expect(isContentManager(createMockUser({ role: 'trade' }))).toBe(false);
          expect(isContentManager(null)).toBe(false);
        });
      });
  - id: 2
    action: verify-fail
    description: Confirm tests fail (requireContentManager not exported)
    command: cd packages/api && bunx vitest run tests/middleware/auth.test.ts
    expected_result: FAIL - requireContentManager is not exported
    status: pending
  - id: 3
    action: implement
    description: Add requireContentManager (below requireAdmin at line ~262) and isContentManager helper (below isAdmin at line ~424)
    file: packages/api/src/middleware/auth.ts
    status: pending
    code: |-
      /**
       * Require content management access (content-manager, admin, or super-admin)
       *
       * Gates product/content management routes.
       * Must be used after requireAuth middleware.
       */
      export const requireContentManager = requireRole([
        "content-manager",
        "admin",
        "super-admin",
      ]);

      /**
       * Check if user can manage content (content-manager, admin, or super-admin)
       */
      export function isContentManager(user: AuthUser | null): boolean {
        return hasAnyRole(user, ["content-manager", "admin", "super-admin"]);
      }
  - id: 4
    action: verify-pass
    description: All middleware tests pass (including pre-existing ones)
    command: cd packages/api && bunx vitest run tests/middleware/auth.test.ts
    expected_result: PASS
    status: pending
  - id: 5
    action: commit
    description: "Commit changes (replace #TICKET with this ticket's number)"
    command: "git add -A && git commit -m \"feat(api): add requireContentManager middleware and isContentManager helper (#TICKET)\""
    status: pending
current_step: 1
started: "2026-07-23T12:01:39.507Z"
completed: "2026-07-23T12:02:44.122Z"
