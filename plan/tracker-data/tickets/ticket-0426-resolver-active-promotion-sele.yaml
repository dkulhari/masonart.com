ticket_number: 426
title: Resolver - active promotion selection with derived state and no stacking
description: |-
  ## Task
  Create `packages/api/src/lib/promotion-pricing.ts` with the active-promotion half: load enabled promotions whose window contains `now`, cache them in-process for 60s, and select ONE for a given product — highest `priority`, ties broken by the deeper discount.

  Promotions never stack. One price per product, always attributable to one promotion row.

  ## Context
  Part of feature: sale-promotions
  Phase: 2 — Resolver
  Design: `docs/superpowers/specs/2026-08-05-sale-promotions-design.md` §5

  Modelled on `packages/api/src/lib/product-sales.ts` — the existing precedent for a derived pricing signal expressed once. Scope matching and price maths land in T5; this ticket is selection and caching only.

  The table is tiny and read on every product request, hence the cache. `now` is injected so tests need no fake timers.
feature: sale-promotions
type: task
priority: high
status: done
assignee: unassigned
reporter: dhruv
created: '2026-08-05T08:01:36.477Z'
last_updated: '2026-08-06T05:27:18.455Z'
time_spent_minutes: 0
labels:
  - backend
  - api
acceptance_criteria: []
comments:
  - author: system
    timestamp: '2026-08-06T05:25:53.060Z'
    text: Status changed from 'todo' to 'in-progress'
  - author: mcp-client
    timestamp: '2026-08-06T05:27:14.988Z'
    text: |-
      ✅ **Completed**

      **Commit**: `cf2b852b` — feat(api): active promotion selection — derived state, no stacking

      **Files Changed**:
      - `packages/api/src/lib/promotion-pricing.ts` (new) — `isPromotionActive`, `selectPromotion`, `getActivePromotions` with a 60s in-process cache, `invalidateActivePromotions`
      - `packages/api/tests/lib/promotion-pricing.test.ts` (new) — 8 tests over derived active state and priority selection

      **Summary**:
      The selection half of the resolver only. Active state is derived from the row (`isEnabled && startsAt <= now && (endsAt is null || endsAt > now)`), so a sale ends on its own — no job, no status column to drift. Overlap resolves to exactly one row: highest `priority`, ties broken by the deeper `discountValue`. Active rows are cached for 60s because the table is tiny and read on every product request; admin writes call `invalidateActivePromotions()` instead of waiting the TTL out. `now` is injected, so the tests need no fake timers.

      `resolveSalePrice` — scope matching and the price maths — is deliberately out of scope; that is #427.

      Style note: implementation follows `src/lib/product-sales.ts` (double quotes, semicolons); the test follows the `tests/lib/` neighbourhood (single quotes, semicolons).

      **Verification**:
      - Command: `cd packages/api && bunx vitest run tests/lib/promotion-pricing.test.ts`
      - Red: FAIL — `Cannot find module '../../src/lib/promotion-pricing'` (the expected failure)
      - Green: PASS — Test Files 1 passed (1), Tests 8 passed (8)
      - Types: `bunx tsc --noEmit` reports zero errors in either new file
  - author: system
    timestamp: '2026-08-06T05:27:18.455Z'
    text: Status changed from 'in-progress' to 'done'
work_sessions: []
related_tickets: []
blocked_by:
  - ticket: 423
    feature: sale-promotions
    description: Schema - promotion, promotion_product and promotion_exclusion tables
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: Write failing tests for derived active state and priority selection
    file: packages/api/tests/lib/promotion-pricing.test.ts
    command: cd packages/api && bunx vitest run tests/lib/promotion-pricing.test.ts
    expected_result: FAIL
    status: done
    code: |-
      import { describe, it, expect } from 'vitest'
      import { isPromotionActive, selectPromotion } from '../../src/lib/promotion-pricing'

      const NOW = new Date('2026-08-10T12:00:00.000Z')

      function promo(overrides: Record<string, unknown> = {}) {
        return {
          id: '0b6c2f7e-6f0e-4a9b-9a52-2a6d3f9c1e11',
          headline: 'SALE',
          discountType: 'percentage',
          discountValue: 40,
          scopeType: 'all',
          scopeFilter: null,
          membersOnly: true,
          startsAt: new Date('2026-08-01T00:00:00.000Z'),
          endsAt: new Date('2026-08-31T00:00:00.000Z'),
          isEnabled: true,
          priority: 0,
          ...overrides,
        } as never
      }

      describe('isPromotionActive', () => {
        it('is active inside its window when enabled', () => {
          expect(isPromotionActive(promo(), NOW)).toBe(true)
        })

        it('is inactive when disabled, whatever the dates say', () => {
          expect(isPromotionActive(promo({ isEnabled: false }), NOW)).toBe(false)
        })

        it('is inactive before it starts', () => {
          expect(isPromotionActive(promo({ startsAt: new Date('2026-09-01T00:00:00.000Z') }), NOW)).toBe(false)
        })

        it('is inactive once it has ended — no job has to switch it off', () => {
          expect(isPromotionActive(promo({ endsAt: new Date('2026-08-09T00:00:00.000Z') }), NOW)).toBe(false)
        })

        it('runs open-ended when endsAt is null', () => {
          expect(isPromotionActive(promo({ endsAt: null }), NOW)).toBe(true)
        })
      })

      describe('selectPromotion', () => {
        it('returns null when nothing is active', () => {
          expect(selectPromotion([])).toBeNull()
        })

        it('picks the highest priority — promotions never stack', () => {
          const chosen = selectPromotion([
            promo({ id: 'a', priority: 1, discountValue: 10 }),
            promo({ id: 'b', priority: 5, discountValue: 5 }),
          ])
          expect(chosen?.id).toBe('b')
        })

        it('breaks a priority tie with the deeper discount', () => {
          const chosen = selectPromotion([
            promo({ id: 'a', priority: 3, discountValue: 20 }),
            promo({ id: 'b', priority: 3, discountValue: 45 }),
          ])
          expect(chosen?.id).toBe('b')
        })
      })
  - id: 2
    action: verify-fail
    description: Confirm the test fails
    command: cd packages/api && bunx vitest run tests/lib/promotion-pricing.test.ts
    expected_result: FAIL - Cannot find module '../../src/lib/promotion-pricing'
    status: done
  - id: 3
    action: implement
    description: Implement active-state derivation, selection and the 60s cache
    file: packages/api/src/lib/promotion-pricing.ts
    status: done
    code: |-
      /**
       * Sale pricing, expressed once.
       *
       * Every surface that shows a price — product list, product detail, cart,
       * order creation, /sale — calls this module. None of them computes a
       * discount, so there is exactly one place where the maths can be wrong.
       *
       * Active state is derived from the row rather than stored, so a sale ends on
       * its own: no job to run, no status column to fall out of sync.
       *
       * Promotions never stack. Overlap resolves to exactly one row.
       */

      import { and, eq, isNull, lte, or, gt } from 'drizzle-orm'
      import { db } from '../database'
      import { promotions } from '../database/schema/promotions'

      export type Promotion = typeof promotions.$inferSelect

      /** Enabled, started, not yet ended. `endsAt` null runs open-ended. */
      export function isPromotionActive(promotion: Promotion, now: Date): boolean {
        if (!promotion.isEnabled) return false
        if (promotion.startsAt > now) return false
        if (promotion.endsAt && promotion.endsAt <= now) return false
        return true
      }

      /** Highest priority wins; ties go to the deeper discount. Never stacks. */
      export function selectPromotion(candidates: Promotion[]): Promotion | null {
        if (candidates.length === 0) return null
        return candidates.reduce((best, candidate) => {
          if (candidate.priority !== best.priority) {
            return candidate.priority > best.priority ? candidate : best
          }
          return candidate.discountValue > best.discountValue ? candidate : best
        })
      }

      const CACHE_TTL_MS = 60_000
      let cache: { at: number; rows: Promotion[] } | null = null

      /** Tiny table, read on every product request. 60s of staleness is acceptable. */
      export async function getActivePromotions(now: Date = new Date()): Promise<Promotion[]> {
        if (cache && now.getTime() - cache.at < CACHE_TTL_MS) return cache.rows

        const rows = await db
          .select()
          .from(promotions)
          .where(
            and(
              eq(promotions.isEnabled, true),
              lte(promotions.startsAt, now),
              or(isNull(promotions.endsAt), gt(promotions.endsAt, now))
            )
          )

        cache = { at: now.getTime(), rows }
        return rows
      }

      /** Admin writes call this so an enable takes effect without a 60s wait. */
      export function invalidateActivePromotions(): void {
        cache = null
      }
  - id: 4
    action: verify-pass
    description: Confirm the tests pass
    command: cd packages/api && bunx vitest run tests/lib/promotion-pricing.test.ts
    expected_result: PASS
    status: done
  - id: 5
    action: commit
    description: Commit the selection half of the resolver
    command: 'git add packages/api/src/lib/promotion-pricing.ts packages/api/tests/lib/promotion-pricing.test.ts && git commit -m "feat(api): active promotion selection — derived state, no stacking"'
    status: done
current_step: 5
started: '2026-08-06T05:25:53.060Z'
completed: '2026-08-06T05:27:18.455Z'
