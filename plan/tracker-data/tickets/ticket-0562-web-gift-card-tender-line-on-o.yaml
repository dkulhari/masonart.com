ticket_number: 562
title: Web - gift card tender line on order confirmation and order detail
description: |-
  ## Task
  Render `giftCardAmount` on the order confirmation screen, customer order detail, and the admin order detail (`components/admin/OrderDetail.tsx`, which already renders `couponCode` / `couponDiscount` at lines 145-146).

  It sits below tax, above the charged total — the same position it holds in the money path. Never in the discount block.

  A gift card order (`orderType: 'gift_card'`) reads differently from a poster order: no shipping section, no tracking, and the line item is the card itself. Make sure those screens do not render an empty shipping panel for it.

  ## Context
  Part of feature: gift-cards
  Phase: 7 — Storefront
  Design: `docs/superpowers/specs/2026-08-06-gift-cards-design.md` §10

  Orders with no gift card render nothing — no zero row.
feature: gift-cards
type: task
priority: low
status: done
assignee: unassigned
reporter: dhruv
created: '2026-08-06T16:34:35.638Z'
last_updated: '2026-08-06T20:19:38.000Z'
time_spent_minutes: 0
labels:
  - frontend
  - ui
acceptance_criteria: []
comments:
  - author: system
    timestamp: '2026-08-06T20:19:38.000Z'
    text: Status changed from 'todo' to 'done'
work_sessions: []
related_tickets: []
blocked_by: []
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: Write failing tests for the tender line
    file: packages/web/tests/components/order/GiftCardTender.test.tsx
    command: cd packages/web && bunx vitest run tests/components/order/GiftCardTender.test.tsx
    expected_result: FAIL
    status: pending
    code: |-
      describe('gift card on order surfaces', () => {
        it('shows the tender below tax, above the charged total', async () => {})
        it('renders nothing when no gift card was used', async () => {
          // no zero row
        })
        it('keeps it out of the discount block', async () => {})
        it('omits shipping and tracking for a gift_card order', async () => {
          // a voucher ships nothing; an empty panel reads as missing data
        })
      })
  - id: 2
    action: verify-fail
    description: Confirm the tests fail
    command: cd packages/web && bunx vitest run tests/components/order/GiftCardTender.test.tsx
    expected_result: FAIL
    status: pending
  - id: 3
    action: implement
    description: Render the tender line across the order surfaces
    file: packages/web/app/components/admin/OrderDetail.tsx
    status: pending
  - id: 4
    action: verify-pass
    description: Confirm the tests pass
    command: cd packages/web && bunx vitest run tests/components/order/GiftCardTender.test.tsx
    expected_result: PASS
    status: pending
  - id: 5
    action: commit
    description: Commit the order surfaces
    command: 'git add packages/web/app packages/web/tests/components/order/GiftCardTender.test.tsx && git commit -m "feat(web): show gift card tender on order surfaces"'
    status: pending
current_step: 1
completed: '2026-08-06T20:19:38.000Z'
