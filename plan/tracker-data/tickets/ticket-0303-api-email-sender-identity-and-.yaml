ticket_number: 303
title: API Email - Sender identity and email templates to chobi.art
description: |-
  ## Task
  Rebrand transactional email: packages/api/src/services/email.ts (DEFAULT_FROM_EMAIL "MasonArt <notifications@masonart.com>" → EMAIL_FROM constant, DEFAULT_REPLY_TO → SUPPORT_EMAIL) and packages/api/src/services/email-templates.ts (34 refs: headers, footers, subject lines, links).

  ## Context
  Part of feature: rebrand-chobi-art
  Phase: 3 - API User-Facing Strings
  Import brand constants from the shared package. Actual Resend sending domain is unchanged until the real chobi.art domain is wired — only display name/copy changes. Existing email template unit tests (if any under packages/api/tests) must be updated in the same commit.
feature: rebrand-chobi-art
type: task
priority: medium
status: done
assignee: unassigned
reporter: cli
created: "2026-07-22T08:08:29.767Z"
last_updated: "2026-07-22T09:07:20.550Z"
time_spent_minutes: 0
labels:
  - backend
  - api
  - email
acceptance_criteria: []
comments:
  - author: system
    timestamp: "2026-07-22T09:03:38.253Z"
    text: Status changed from 'todo' to 'in-progress'
  - author: system
    timestamp: "2026-07-22T09:07:20.564Z"
    text: Status changed from 'in-progress' to 'done'
  - author: mcp-client
    timestamp: "2026-07-22T09:07:49.531Z"
    text: "✅ TDD: flipped email test assertions first (4 failed red), then swept email.ts sender defaults + email-templates.ts (all 34 refs). 75 tests pass. Design deviation: api does NOT depend on @masonart/shared, so brand strings were replaced directly instead of importing constants — adding the workspace dep would ripple into the api Docker build for one string. Web-side keeps the shared constants as source of truth. Commit: 1e40b052."
work_sessions: []
related_tickets: []
blocked_by:
  - ticket: 298
    feature: rebrand-chobi-art
    description: Brand Constants - Create brand.ts with chobi.art identity in shared package
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: Update/add email template test asserting chobi.art branding
    file: packages/api/tests/services/email-templates.test.ts
    command: bunx vitest run packages/api/tests --testNamePattern brand
    expected_result: FAIL
    status: pending
    code: |-
      // Assert rendered template contains "chobi.art" and not "MasonArt", e.g.:
      // const html = orderConfirmationTemplate(sampleOrder);
      // expect(html).toContain("chobi.art");
      // expect(html).not.toContain("MasonArt");
  - id: 2
    action: verify-fail
    description: Confirm test fails against current templates
    command: bunx vitest run packages/api/tests
    expected_result: FAIL - templates still say MasonArt
    status: pending
  - id: 3
    action: implement
    description: Swap email.ts sender constants and sweep email-templates.ts
    file: packages/api/src/services/email.ts
    status: pending
    code: |-
      import { EMAIL_FROM, SUPPORT_EMAIL, BRAND_NAME, BRAND_URL } from "@masonart/shared";
      const DEFAULT_FROM_EMAIL = process.env.EMAIL_FROM || EMAIL_FROM;
      const DEFAULT_REPLY_TO = process.env.EMAIL_REPLY_TO || SUPPORT_EMAIL;
      // email-templates.ts: replace all 34 MasonArt/masonart.com refs with BRAND_NAME/BRAND_URL interpolation
  - id: 4
    action: verify-pass
    description: API unit tests pass
    command: bunx vitest run packages/api/tests
    expected_result: PASS
    status: pending
  - id: 5
    action: commit
    description: Commit email rebrand
    command: "git add packages/api && git commit -m \"feat(api): chobi.art email sender and templates (#TICKET)\""
    status: pending
current_step: 1
started: "2026-07-22T09:03:38.251Z"
completed: "2026-07-22T09:07:20.550Z"
