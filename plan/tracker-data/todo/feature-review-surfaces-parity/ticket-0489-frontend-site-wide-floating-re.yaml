ticket_number: 489
title: Frontend - site-wide floating review toast
description: |-
  ## Task

  Mesonart runs a bottom-left toast that cycles recent reviews on every page. Ours does the same, mounted once in the root route, dismissible, and remembered for the session.

  ## Context

  Part of feature: review-surfaces-parity
  Phase: Frontend
  Depends on: the client layer

  - Mount in `packages/web/app/routes/__root.tsx` so it survives navigation and fetches once rather than per route
  - **Suppressed on `/checkout`** and on any admin route — a social-proof popup over a payment form is a conversion and trust problem, not a feature
  - Mobile: must sit **under** the cart drawer's z-index and clear of the sticky add-to-cart bar on the PDP. Check the drawer's z-index before picking one rather than guessing
  - `sessionStorage`, not `localStorage` — dismissal lasts the session, not forever
  - **`sessionStorage` needs a `vi.hoisted` stub in tests.** Node 25's inert `localStorage`/`sessionStorage` shadows jsdom's and throws `setItem is not a function`
  - Respect `prefers-reduced-motion`: no slide-in animation when it is set
feature: review-surfaces-parity
type: task
priority: low
status: todo
assignee: unassigned
reporter: dhruv
created: '2026-08-05T15:15:25.097Z'
last_updated: '2026-08-05T15:15:25.097Z'
time_spent_minutes: 0
labels:
  - frontend
  - ui
acceptance_criteria: []
comments: []
work_sessions: []
related_tickets: []
blocked_by:
  - ticket: 484
    feature: review-surfaces-parity
    description: Frontend - API client functions and hooks for review feed and media
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: Write failing tests for the toast
    file: packages/web/tests/components/reviews/ReviewToast.test.tsx
    command: cd packages/web && bun run test -- ReviewToast
    expected_result: FAIL
    status: pending
    code: |-
      // Stub storage in vi.hoisted ABOVE the imports - see the ticket note.
      // renders a review after the initial delay and cycles to the next on an interval
      // dismissing hides it and writes the sessionStorage flag
      // a set flag suppresses it on mount, with no fetch at all
      // renders nothing on /checkout and on /admin routes
      // no slide animation class when prefers-reduced-motion matches
      // clicking the toast navigates to that review's product
  - id: 2
    action: verify-fail
    description: Confirm the test fails
    command: cd packages/web && bun run test -- ReviewToast
    expected_result: FAIL - component not found
    status: pending
  - id: 3
    action: implement
    description: Build the toast
    file: packages/web/app/components/reviews/ReviewToast.tsx
    status: pending
    code: |-
      // Route check first, before any data fetch:
      //   const { pathname } = useRouterState({ select: s => s.location })
      //   if (pathname.startsWith('/checkout') || pathname.startsWith('/admin')) return null
      // Then the sessionStorage dismissal flag, then useReviewFeed(1, 10).
      // Cycle every ~8s; 4s initial delay. Card: thumbnail (media or product image),
      //   star row, one-line excerpt, author, close button.
      // Fixed bottom-left, z-index BELOW the cart drawer (read the drawer's value,
      //   do not guess), and offset above the PDP sticky bar on small screens.
  - id: 4
    action: implement
    description: Mount the toast in the root route
    file: packages/web/app/routes/__root.tsx
    status: pending
    code: |-
      // Render <ReviewToast /> once inside the root layout, after the main outlet so
      // it stacks above page content but below the drawer.
  - id: 5
    action: verify-pass
    description: Confirm the toast tests pass
    command: cd packages/web && bun run test -- ReviewToast
    expected_result: PASS
    status: pending
  - id: 6
    action: commit
    description: Commit the toast
    command: 'git add packages/web/app/components/reviews/ReviewToast.tsx packages/web/app/routes/__root.tsx packages/web/tests/components/reviews/ReviewToast.test.tsx && git commit -m "feat(web): site-wide review toast"'
    status: pending
current_step: 1
