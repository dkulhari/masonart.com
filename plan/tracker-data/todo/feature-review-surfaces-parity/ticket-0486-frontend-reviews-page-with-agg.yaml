ticket_number: 486
title: Frontend - /reviews page with aggregate header, plus nav, footer and sitemap links
description: |-
  ## Task

  The destination page. Aggregate score band over a reverse-chronological list of every approved review, each linking back to the product it reviews. Linked from the header nav, the footer, and the sitemap — mesonart surfaces it in all three.

  ## Context

  Part of feature: review-surfaces-parity
  Phase: Frontend
  Depends on: the client layer

  - **`validateSearch` must coerce `page` to a number.** `packages/web/app/routes/router.tsx` overrides TanStack's search serialisation so every param arrives as a string; a bare `z.number()` throws and the route error-boundaries to a blank page
  - Adding a route rewrites `packages/web/app/routeTree.gen.ts`, which is **tracked, not gitignored**. Stay in one checkout and `git add` by explicit path
  - Aggregate header reuses the suppression rule: render nothing below 10 approved reviews rather than rounding a thin sample into a marketing number. `GET /api/reviews/stats` already returns `averageRating: null` for that case — honour the null, do not coalesce it to 0
  - Follow `packages/web/app/routes/wishlist.tsx` for route-file shape and `SectionBand` / `DisplayHeading` for the beige header band
  - Header nav links live in `packages/web/app/components/layout/Header.tsx` (desktop ~line 376 and mobile ~line 504 — both trees need the link); footer in `Footer.tsx`; sitemap in `packages/api/src/routes/sitemap.ts`
feature: review-surfaces-parity
type: task
priority: medium
status: todo
assignee: unassigned
reporter: dhruv
created: '2026-08-05T15:14:24.199Z'
last_updated: '2026-08-05T15:14:24.199Z'
time_spent_minutes: 0
labels:
  - frontend
  - ui
acceptance_criteria: []
comments: []
work_sessions: []
related_tickets: []
blocked_by:
  - ticket: 484
    feature: review-surfaces-parity
    description: Frontend - API client functions and hooks for review feed and media
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: Write failing tests for the reviews page
    file: packages/web/tests/routes/reviews.test.tsx
    command: cd packages/web && bun run test -- routes/reviews
    expected_result: FAIL
    status: pending
    code: |-
      // validateSearch coerces the string '2' to the number 2 and defaults to 1
      // the aggregate header renders score and count when reviewCount >= 10
      // the header renders nothing when averageRating is null or count < 10
      // each row shows rating, title, body, author, date and a product link
      // the empty state renders without throwing
  - id: 2
    action: verify-fail
    description: Confirm the tests fail
    command: cd packages/web && bun run test -- routes/reviews
    expected_result: FAIL - route module does not exist
    status: pending
  - id: 3
    action: implement
    description: Create the reviews route
    file: packages/web/app/routes/reviews.tsx
    status: pending
    code: |-
      // export const Route = createFileRoute('/reviews')({
      //   validateSearch: z.object({
      //     page: z.coerce.number().int().min(1).catch(1),   // coerce, see ticket note
      //   }),
      //   component: ReviewsPage,
      // })
      // Beige SectionBand header: DisplayHeading "Reviews & Ratings" + the aggregate
      // strip (score, star row, "N reviews") under the >= 10 suppression rule.
      // Body: useReviewFeed(page) -> ReviewCard list, media thumbnails inline,
      // prev/next paging that writes ?page= through the router.
  - id: 4
    action: implement
    description: Add the nav, footer and sitemap links
    file: packages/web/app/components/layout/Header.tsx
    status: pending
    code: |-
      // Header.tsx: add a Reviews link to BOTH the desktop nav (~:376) and the mobile
      //   nav (~:504) - the same ids exist in both trees and only patching one is the
      //   classic miss here.
      // Footer.tsx: "Reviews & Ratings" under Customer Services, matching mesonart.
      // packages/api/src/routes/sitemap.ts: add the /reviews URL entry.
  - id: 5
    action: verify-pass
    description: Confirm tests pass and the route tree regenerated
    command: cd packages/web && bun run test -- routes/reviews && git diff --stat packages/web/app/routeTree.gen.ts
    expected_result: PASS, and routeTree.gen.ts shows the /reviews route added
    status: pending
  - id: 6
    action: commit
    description: Commit the reviews page and its links
    command: 'git add packages/web/app/routes/reviews.tsx packages/web/app/routeTree.gen.ts packages/web/app/components/layout/Header.tsx packages/web/app/components/layout/Footer.tsx packages/api/src/routes/sitemap.ts packages/web/tests/routes/reviews.test.tsx && git commit -m "feat(web): reviews page with aggregate header and nav links"'
    status: pending
current_step: 1
