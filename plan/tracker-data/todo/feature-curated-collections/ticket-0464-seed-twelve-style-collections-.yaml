ticket_number: 464
title: Seed - twelve style collections plus Latest Work and Best Sellers
description: |-
  ## Task

  Seed fourteen collections so the Discover rail cannot empty out the moment it stops reading `STYLE_OPTIONS`:

  - Twelve rule collections, one per `STYLE_OPTIONS` entry — slug from the style id, rule `{ styles: [id] }`, `showInDiscover: true`, `discoverOrder` following the vocabulary order.
  - **Latest Work** — slug `new`, rule `{ sortBy: 'createdAt', sortOrder: 'desc' }`, no facets.
  - **Best Sellers** — slug `best-selling`, rule `{ sortBy: 'salesCount', sortOrder: 'desc' }`, no facets.

  Those last two are the whole point of the feature: they are the two mesonart chips that no facet vocabulary can express.

  Idempotent on slug — re-running the seed must not duplicate or clobber an admin's later edits to title/image.

  ## Context

  Feature: curated-collections, Phase 2. Depends on the schema and the shared contracts.

  Find the existing seed entry point under `packages/api/src` and follow its structure. #414 seeded settled orders and approved reviews; match that shape.

  The twelve style collections preserve today's twelve chips in link form, so the storefront change is not also a content change.
feature: curated-collections
type: task
priority: high
status: todo
assignee: unassigned
reporter: dhruv
created: '2026-08-05T13:49:55.457Z'
last_updated: '2026-08-05T13:49:55.457Z'
time_spent_minutes: 0
labels:
  - backend
  - database
  - seed
acceptance_criteria: []
comments: []
work_sessions: []
related_tickets: []
blocked_by:
  - ticket: 462
    feature: curated-collections
    description: Schema - collections and collection_products tables
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: Write failing seed tests
    file: packages/api/tests/seed/collections-seed.test.ts
    command: cd packages/api && npx vitest run tests/seed/collections-seed.test.ts
    expected_result: FAIL
    status: pending
    code: 'Cases: produces 14 rows; one per STYLE_OPTIONS id plus ''new'' and ''best-selling''; every style rule names a style that exists in the vocabulary; the two merchandising rules carry a sort and no facets; running twice yields 14, not 28; a row whose title was edited is not reset by a second run.'
  - id: 2
    action: verify-fail
    description: Confirm failure
    command: cd packages/api && npx vitest run tests/seed/collections-seed.test.ts
    expected_result: FAIL
    status: pending
  - id: 3
    action: implement
    description: Write the seed module and wire it into the seed entry point
    status: pending
    code: 'Derive the twelve from STYLE_OPTIONS rather than listing them — a hardcoded copy here is the drift #395 ended. Upsert on slug, only filling columns the admin has not set.'
  - id: 4
    action: verify-pass
    description: Confirm the seed tests pass
    command: cd packages/api && npx vitest run tests/seed/collections-seed.test.ts
    expected_result: PASS
    status: pending
  - id: 5
    action: commit
    description: Commit
    command: 'git add packages/api/src packages/api/tests/seed && git commit -m "feat(api): seed style collections plus Latest Work and Best Sellers (#TICKET)"'
    status: pending
current_step: 1
