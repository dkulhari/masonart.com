ticket_number: 473
title: Admin - collection form with rule builder and manual picker
description: |-
  ## Task

  `/admin/collections/new` and `/admin/collections/$id` — one form, both modes.

  Fields: title, slug (suggested from the title, editable, checked for collision before submit), subtitle, description, image URL, active, show-in-discover, SEO title and description.

  Kind switch:

  - **Rule** — a builder over the shared facet vocabulary: multi-select per facet group, price range, the AI/featured flags, and a default sort from `SORT_OPTIONS`. Every option comes from `@chobii/shared`; a hardcoded list here is the drift #395 ended.
  - **Manual** — a product search-and-pick list with drag ordering. Order is the data.

  Live count preview so the admin sees what a rule resolves to before saving — a rule that matches nothing is the failure mode worth catching at authoring time.

  Switching kind after products or a rule exist warns that the other side's data will be dropped, and does not drop it until save.

  ## Context

  Feature: curated-collections, Phase 6. Depends on the admin API tickets.

  Follow `packages/web/app/components/admin/ProductForm.tsx` for form idiom, validation surfacing and the create/edit split.

  The rule builder emits exactly `collectionRuleSchema`. Validate client-side with the same schema the API validates with, so the two cannot disagree.
feature: curated-collections
type: task
priority: medium
status: todo
assignee: unassigned
reporter: dhruv
created: '2026-08-05T13:51:59.859Z'
last_updated: '2026-08-05T13:51:59.859Z'
time_spent_minutes: 0
labels:
  - frontend
  - ui
  - admin
acceptance_criteria: []
comments: []
work_sessions: []
related_tickets: []
blocked_by:
  - ticket: 467
    feature: curated-collections
    description: API - admin CRUD for collections
  - ticket: 468
    feature: curated-collections
    description: API - manual membership and Discover ordering
blocks: []
attachments: []
implementation_steps:
  - id: 1
    action: write-test
    description: Write failing form tests
    file: packages/web/tests/components/admin/CollectionForm.test.tsx
    command: cd packages/web && npx vitest run tests/components/admin/CollectionForm.test.tsx
    expected_result: FAIL
    status: pending
    code: 'Cases: kind=rule shows the builder and hides the picker, and vice versa; facet options come from the shared vocabularies; the emitted payload parses under collectionRuleSchema; slug is suggested from the title but stays editable; a colliding slug surfaces the 409 rather than a generic error; switching kind warns before discarding; the count preview refetches when the rule changes.'
  - id: 2
    action: verify-fail
    description: Confirm failure
    command: cd packages/web && npx vitest run tests/components/admin/CollectionForm.test.tsx
    expected_result: FAIL
    status: pending
  - id: 3
    action: implement
    description: Build the form and both routes
    file: packages/web/app/components/admin/CollectionForm.tsx
    status: pending
    code: CollectionForm plus routes/admin/collections/new.tsx and $id.tsx.
  - id: 4
    action: verify-pass
    description: Confirm they pass and typecheck is clean
    command: cd packages/web && npx vitest run tests/components/admin/CollectionForm.test.tsx && npx tsc --noEmit -p tsconfig.json
    expected_result: PASS
    status: pending
  - id: 5
    action: commit
    description: Commit
    command: 'git add packages/web/app/components/admin/CollectionForm.tsx packages/web/app/routes/admin/collections packages/web/tests/components/admin && git commit -m "feat(web): admin collection form with rule builder and manual picker (#TICKET)"'
    status: pending
current_step: 1
