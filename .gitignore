# Auto Claude data directory
.auto-claude/

# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
bundle-stats.html
build/
.output/
.vinxi/
.next/
out/
.tanstack/

# Bun
bun.lockb

# Turbo
.turbo/

# Testing
coverage/
*.lcov
playwright-report/
test-results/
.playwright-mcp/

# TypeScript
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Timestamped backups — same secrets as .env, and nothing stops another one
# being written.
.env.bak-*

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Drizzle
drizzle/meta/

# Docker volumes (local)
docker/data/

# Misc
.cache/
*.pem
*.key

# Database dumps — may contain auth material (password hashes, session tokens).
# Deliberately narrow: Drizzle migrations and deploy/*.sql scripts stay tracked.
deploy/pre-deploy-*.sql
*.dump

# Git worktrees
.worktrees/

# Claude mem context files
CLAUDE.md

# Local config files
.mcp.json

# Production deploy — secrets and rendered compose never enter git
deploy/.env
deploy/rendered/
deploy/pre-cutover-*.sql

# ticketrack MCP per-process stdio log — local pids, no shared value
plan/tracker-data/.mcp-exits.jsonl

# Development reference imagery scraped from mesonart.com to build the home
# page bands against. Not ours, must never ship or enter history. Ticket #544
# replaces it with our own photography. The fetch script that repopulates it
# is ignored alongside it.
packages/web/public/dev-reference/
scripts/dev/fetch-reference-imagery.sh
